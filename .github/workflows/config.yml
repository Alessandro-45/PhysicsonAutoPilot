# Panel de control para el análisis de física
analysis_parameters:
  # Esto es lo que el retador mencionó: "corte de 1 a 0"
  corte_min: 0.0
  corte_max: 1.0
  steps: 0.5

# Otros parámetros que el Físico pueda necesitar
dataset:
  nombre: "datos_run_1234.root"
  eventos_a_procesar: 10000